{
  "compilerOptions": {
    "allowJs": true,
    "target": "ES5",
    "outDir": "./built", // 타입스크립트 결과물이 어디에 들어갈 것인가
    "moduleResolution": "Node", // Promise 인식시켜주기 위해 필요
    "lib": ["ES2015", "DOM", "DOM.Iterable"],
    "noImplicitAny": true
  },
  "include": ["./src/**/*"] // 프로젝트를 기준으로 해서 어떤 폴더를 대상으로 타입스크립트를 컴파일 시킬 것인지,여기서는 src 폴더 및 모든 파일을 대상으로 하겠다고 설정
}
